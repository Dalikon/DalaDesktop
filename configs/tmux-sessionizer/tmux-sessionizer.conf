  # Search these paths (replaces defaults)                            
  TS_SEARCH_PATHS=(~/Documents ~/Documents/school/MIT/4.semestr/NAV/projects)
  # Or add to defaults (with optional depth :N)                       
  TS_EXTRA_SEARCH_PATHS=(~/ghq:3 ~/projects:2)                        
  # Max search depth (default: 1)                                     
  TS_MAX_DEPTH=1                                                      
  # Commands for -s flag (window 69+)                                 
  TS_SESSION_COMMANDS=(htop "nvim ~/notes" "lazygit")                 
  # Enable logging                                                    
  TS_LOG=true
